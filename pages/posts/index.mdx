import preval from 'babel-plugin-preval/macro';
import PostHeader from '../../components/PostHeader';
import PostHeaderList from '../../components/PostHeaderList';

export const metadata = {
  title: 'All Posts',
};

<PostHeader {...metadata} />

Sometimes I write about things that I've been doing or that interest me. This is where those writings go.

export const subpages = preval`module.exports = require('../../data/metaGetter').getFeaturedPages('posts')`;

<PostHeaderList pages={subpages} />
